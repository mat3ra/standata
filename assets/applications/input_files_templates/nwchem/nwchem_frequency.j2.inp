 {% if input.RESTART %}restart{% else %}start{% endif %} nwchem
 title "Test"
 charge {{ input.CHARGE }}
{%- if not input.RESTART %}
 geometry units angstrom noautosym
 {{ input.ATOMIC_POSITIONS }}
 end
{%- endif %}
 basis
   * library {{ input.BASIS }}
 end
 dft
 xc {{ input.FUNCTIONAL }}
 mult {{ input.MULT }}
 end
 driver
 maxiter 256
 inhess 1
 print default
 end
 # Uncomment the following line to perform a geometry optimization before the frequency calculation
 # task dft optimize
 task dft freq
