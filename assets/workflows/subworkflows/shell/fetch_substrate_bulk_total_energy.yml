# Fetch precomputed substrate (bulk A) total energy from a finished Total Energy job.
#
# In:  BULK_A_MATERIAL_IDS
# Out: E_BULK_A
application:
  name: shell
method:
  name: UnknownMethod
model:
  name: UnknownModel
name: Fetch Substrate Total Energy
units:
  - type: assignment
    config:
      name: assign-bulk-material-ids
      flowchartId: assign-bulk-material-ids-substrate
      operand: BULK_MATERIAL_IDS
      value: "BULK_A_MATERIAL_IDS"
  - type: io
    config:
      name: io-bulk-te-job-a
      flowchartId: io-bulk-te-job-a
      subtype: input
      source: api
      input:
        - type: api
          endpoint: jobs
          endpoint_options:
            params:
              query: >-
                {'_material._id': {'$in': BULK_MATERIAL_IDS}, 'status': 'finished',
                'workflow.name': {'$regex': 'Total Energy'}}
              projection: "{'sort': {'updatedAt': -1}, 'limit': 1}"
          name: JOB_DATA
  - type: assignment
    config:
      name: assign-bulk-te-job-id-a
      flowchartId: assign-bulk-te-job-id-a
      operand: BULK_TE_JOB_ID
      value: "JOB_DATA[0]['_id'] if JOB_DATA else None"
      input:
        - name: JOB_DATA
          scope: io-bulk-te-job-a
  - type: io
    config:
      name: io-e-bulk-a
      flowchartId: io-e-bulk-a
      subtype: input
      source: api
      input:
        - type: api
          endpoint: properties
          endpoint_options:
            params:
              query: >-
                {'source.info.jobId': BULK_TE_JOB_ID, 'slug': 'total_energy',
                'group': {'$regex': 'qe:'}}
              projection: "{'sort': {'precision.value': -1}, 'limit': 1}"
          name: DATA
  - type: assignment
    config:
      name: assign-e-bulk-a
      flowchartId: assign-e-bulk-a
      operand: E_BULK_A
      value: "DATA[0].data.value if DATA else None"
      input:
        - name: DATA
          scope: io-e-bulk-a
  - type: assertion
    config:
      name: assert-e-bulk-a
      flowchartId: assert-e-bulk-a
      statement: "E_BULK_A != None"
      errorMessage: >-
        Bulk total energy not found for substrate (A).
        Run a Total Energy workflow on bulk A first.
