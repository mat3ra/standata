---

name: Publish WIP release tarball

on: push

jobs:
  release-wip:
    uses: mat3ra/actions/.github/workflows/release-wip.yml@main
    with:
      package-name: standata
      build-script: build
    secrets:
      github-token: ${{ secrets.BOT_GITHUB_TOKEN }}
